aws sts get-caller-identity #checks user credentials in aws
aws configure #setup your access keys etc here. 
aws configure get region #see which region are you logged in into. 
aws bedrock list-foundation-models --region us-east-2
